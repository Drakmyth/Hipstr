<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="HipstrMessageSourceListItemTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" />
            <StackPanel Grid.Column="1">
                <TextBlock Text="{Binding Name, Mode=OneTime}"
                           Foreground="{ThemeResource HipstrText}" />
                <TextBlock Text="{Binding Team.Name, Mode=OneTime}"
                           Foreground="{ThemeResource HipstrSubText}"
                           FontSize="14" />
            </StackPanel>
        </Grid>
    </DataTemplate>
    <Style x:Key="HipstrListButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{ThemeResource HipstrText}" />
    </Style>
    <Style x:Key="HipstrStretchList" TargetType="ListViewItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
    </Style>
</ResourceDictionary>